# متابعة خلل المزامنة والتقارير

- [ ] جمع مخرجات عامل Android كاملةً مع وضع المزامنة وعدد المستخدمين والجلسات والبروفايلات.
- [ ] فحص أحدث `sync_runs` و`report_snapshots` وعداد صفوف `report_users` في Supabase.
- [ ] مقارنة عدد المستخدمين المرفوع بعدة اللقطة النشطة الذي تقرؤه دالة `reports`.
- [ ] تدقيق حفظ جميع الصفوف في دفعات الإدخال وعدم وجود فشل صامت أو تعارض مفاتيح.
- [ ] نشر الإصلاح والتحقق من مزامنة Bootstrap جديدة ثم تقرير الصفحات.

## نتائج أولية

- [x] مشروع Supabase `eshzhddotaintyduopko` سليم وترحيلات V2 الثلاث مطبقة.
- [x] دوال Edge الثلاث منشورة، وموقع Cloudflare يعمل ويصل إلى صفحة الدخول.
- [ ] نتيجة فحص SQL للقطات لم تُستخرج بعد من لوحة Supabase؛ واجهة محرر SQL في المتصفح لم تعرض نتيجة الاستعلام بعد التنفيذ.
- [x] جدول `report_users` يحتوي فعلياً على 8,240 صفاً؛ مشكلة حد 1,000 صف ليست سبب العرض الناقص الحالي.
- [ ] فحص حقول `profile` و`nas_port_id` داخل صف مباشر من الجدول وتحديد هل الفراغ يُكتب من العامل أم يسببه عرض التقرير.

## دليل من البيانات الحية

- [x] صف مباشر في `report_users` للمستخدم `7139940` يحمل `profile = EMPTY` و`price = 0` داخل Supabase.
- [x] هذا يستبعد واجهة Cloudflare ودالة `reports` كسبب أولي: العامل أو منطق استخراج RouterOS يرسل قيماً فارغة قبل الحفظ.
- [x] تم تأكيد أن عامل Android شُغّل مع `SYNC_MODE=bootstrap`؛ فراغ NAS ليس نتيجة وضع incremental.

## السبب المؤكد

- [x] عينة المستخدم `62723715` من RouterOS 6 تحتوي فعلياً على `actual-profile = 250` وعلى جلسات تاريخية تحوي `nas-port-id` مثل `HR(7-12)`.
- [x] ربط جلسات RouterOS 6 يتم عبر حقل `user`، بينما استعلام `?username` للجلسات لا يعيد سجلات لهذا الراوتر.
- [x] أسلوب `.proplist` في العامل لا يعيد الحقول الديناميكية المطلوبة بصورة موثوقة لهذا الراوتر رغم أنها تظهر في السجل الكامل.
- [ ] تعديل عامل RouterOS 6 لقراءة السجلات الكاملة ثم ترشيح البيانات محلياً، مع تحذير يمنع نجاح لقطة بلا بروفايلات.
